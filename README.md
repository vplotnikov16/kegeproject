# KEGE Project - Экзаменационная система ЕГЭ

**KEGE Project** - это веб-приложение для управления экзаменационными вариантами и отслеживания результатов тестирования учащихся на Единый государственный экзамен (ЕГЭ).

## Оглавление

- [Особенности](#-особенности)
- [Требования](#-требования)
- [Установка](#-установка)
- [Запуск приложения](#-запуск-приложения)
- [Архитектура](#-архитектура)
- [Структура проекта](#-структура-проекта)
- [База данных](#-база-данных)
- [API и маршруты](#-api-и-маршруты)
- [Dashboard и аналитика](#-dashboard-и-аналитика)
- [Тестирование](#-тестирование)
- [Разработка](#-разработка)
- [Документация](#-документация)
- [Внесение вклада](#-внесение-вклада)
- [Лицензия](#-лицензия)

## ✨ Особенности

### Основной функционал
- **Управление заданиями** — создание, редактирование и удаление задач с HTML-содержимым
- **Создание экзаменационных вариантов** — составление вариантов из задач с указанием времени на экзамен
- **Система попыток** — отслеживание попыток решения вариантов пользователями
- **Подробные результаты** — ответы на каждую задачу, правильность решения, баллы
- **Управление пользователями** — регистрация, аутентификация, профили
- **Система ролей** — поддержка различных уровней доступа (администратор, учитель, ученик)

### Аналитика и статистика
- **Интерактивный Dashboard** — визуализация ключевых метрик на главной странице
- **Графики на Chart.js** — круговые диаграммы, линейные графики и визуализация динамики успеваемости
- **Рейтинги пользователей** — топ по результатам за выбранный период
- **Анализ распределения оценок** — визуализация результатов по диапазонам баллов
- **Еженедельные тренды** — отслеживание среднего балла по неделям
- **Активность системы** — новые пользователи, задачи, варианты и попытки за период

### Технологический стек
- **Flask** — легкий и гибкий веб-фреймворк
- **SQLAlchemy ORM** — объектно-реляционное отображение с поддержкой миграций
- **Flask-Login** — управление сеансами и аутентификацией
- **Bootstrap 5** — адаптивный интерфейс
- **Chart.js** — интерактивные графики
- **WTForms / Flask-WTF** — формы и валидация
- **Pytest** — модульное и интеграционное тестирование
- **Pylint** — контроль качества кода
- **Poetry** — управление зависимостями и окружением

## Требования

### Обязательные
- **Python** 3.10 или выше
- **Poetry** (для управления зависимостями)
- **Git**

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/vplotnikov16/kegeproject.git
cd kegeproject
```

### 2. Установка зависимостей

### Вариант 1. Через `run_in_venv.py` (удобный способ)

В проекте есть вспомогательный скрипт `run_in_venv.py`, который:
- создаёт/активирует виртуальное окружение,
- устанавливает зависимости через Poetry,
- запускает приложение.

```bash
python run_in_venv.py
```

Рекомендуется запускать его через системный интерпретатор Python, чтобы Poetry корректно управлял окружением. Внутри `main()` есть флаг `is_dev_env`, который управляет установкой dev-зависимостей.

### Вариант 2. Напрямую через `run.py`

```bash
# В уже активированном окружении/через Poetry
poetry run python run.py
```

После запуска приложение будет доступно по адресу:

```text
http://172.0.0.1:5000
```

## Архитектура

Архитектура проекта - классический Flask-приложение с разделением на слои:

- **routes (Blueprints)** - обработчики HTTP-запросов, минимальная логика
- **services** - бизнес-логика и агрегирующие операции
- **models** - модели SQLAlchemy и связь с БД
- **templates** - Jinja2-шаблоны, HTML-интерфейс
- **static** - CSS/JS/изображения

## Структура проекта (ключевые части)

```text
kegeproject/
├── app/
│   ├── __init__.py            # создание Flask-приложения, регистрация blueprints
│   ├── config.py              # конфигурация
│   ├── extensions.py          # db, migrate, login_manager
│   ├── cli.py                 # CLI-команды (seed и пр.)
│   │
│   ├── models/                # модели БД
│   │   ├── users.py
│   │   ├── tasks.py
│   │   ├── variants.py
│   │   ├── attempts.py
│   │   ├── attempt_answers.py
│   │   ├── roles.py
│   │   ├── user_roles.py
│   │   ├── task_attachments.py
│   │   ├── variant_tasks.py
│   │   └── user_avatars.py
│   │
│   ├── routes/                # маршруты (blueprints)
│   │   ├── pages.py           # главная, login, register, logout
│   │   ├── auth.py            # API аутентификации
│   │   ├── tasks.py           # CRUD задач
│   │   ├── variants.py        # CRUD вариантов
│   │   ├── profile.py         # профиль пользователя
│   │   ├── users.py           # управление пользователями
│   │   ├── attachments.py     # вложения
│   │   ├── error_handlers.py  # кастомные ошибки
│   │   └── api/
│   │       ├── tasks_api.py
│   │       ├── profile_api.py
│   │       └── variant_api.py
│   │
│   ├── services/              # бизнес-логика
│   │   ├── task_services.py
│   │   ├── variant_services.py
│   │   └── dashboard_service.py   # сбор статистики для главной страницы
│   │
│   ├── templates/             # Jinja2 шаблоны
│   │   ├── base.html
│   │   ├── index.html         # dashboard с графиками и статистикой
│   │   ├── login.html
│   │   ├── register.html
│   │   ├── error.html
│   │   ├── profile/
│   │   ├── tasks/
│   │   ├── users/
│   │   └── variants/
│   │
│   ├── static/
│   │   ├── css/
│   │   ├── js/
│   │   └── img/
│   │
│   └── utils/                 # вспомогательные функции (даты, текст и пр.)
│
├── migrations/                # миграции Alembic
├── tests/                     # тесты (pytest)
├── run.py                     # основной скрипт запуска
├── run_in_venv.py             # удобный запуск в venv через Poetry
├── run_tests_local.py         # локальный запуск тестов
├── pyproject.toml             # конфиг Poetry
├── pytest.ini                 # конфиг pytest
├── .pylintrc                  # конфиг pylint
└── README.md                  # вы это читаете :)
```

## База данных

Основные сущности:

- **User** — пользователь системы (учитель/ученик/админ)
- **Task** — отдельная задача ЕГЭ (с HTML-условием и ответом)
- **Variant** — экзаменационный вариант (набор задач)
- **VariantTask** — связь «задача в варианте»
- **Attempt** — попытка решения варианта
- **AttemptAnswer** — ответ на конкретную задачу в попытке
- **Role / UserRole** — роли и связь пользователь–роль
- **UserAvatar** — аватар пользователя
- **TaskAttachment** — вложения к задачам (файлы, картинки и т.д.)

## API и маршруты (общее представление)

Некоторые ключевые маршруты (подробности см. в коде):

- `GET /` — главная страница, dashboard со статистикой и графиками
- `GET /login`, `POST /login` — вход
- `GET /register`, `POST /register` — регистрация
- `GET /logout` — выход
- `GET /tasks` — список задач
- `GET /variants` — список вариантов
- `GET /profile` — профиль текущего пользователя

REST API (под `app.routes.api`):
- `/api/tasks/...`
- `/api/profile/...`
- `/api/variants/...`

## Dashboard и аналитика

Главная страница (`pages.index`) теперь показывает разнообразную статистику, используя `DashboardService` и графики на базе Chart.js.

### Что отображается на главной странице

- Общее количество пользователей, задач, вариантов и попыток
- Новые пользователи, задачи, варианты и попытки за последние 7 дней
- Последний решённый вариант (кто решал, когда, с каким результатом)
- Распределение оценок за последние 30 дней
- Средний балл по неделям (график тренда)
- Топ-результаты пользователей за последние 30 дней
- Последние добавленные задачи и варианты

## 🧪 Тестирование

Запуск всех тестов:

```bash
poetry run python run_tests_local.py
```
